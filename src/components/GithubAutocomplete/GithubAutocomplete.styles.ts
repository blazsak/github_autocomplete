import styled from 'styled-components';

export const GithubAutocompleteWrapper = styled.div`
    width: calc(100% - 20px);
    max-width: 340px;
    margin: 0 auto;
    text-align: left;
    position: relative;
`;
